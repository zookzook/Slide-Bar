#ifndef _MODE_4_H
#define _MODE_4_H

/**
 * mode 4:
 * 
 * Zini mode. When the value changes, the cursor moves, pulling a tail behind it. The previously visited LEDs are slowly faded out.
 */
void run_mode_4(void);
void enter_mode_4(void);

#endif
